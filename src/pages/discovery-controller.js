(function () {
    keylolApp.controller('DiscoveryController', [
        'pageHead', '$scope', 'union', '$http', 'notification', 'window', 'utils', '$timeout', '$location',
        '$rootScope', 'articleTypes',
        (pageHead, $scope, union, $http, notification, window, utils, $timeout, $location,
        $rootScope, articleTypes) => {
            
        },
    ]);
}());
